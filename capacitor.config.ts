import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.myReactCapacitorApp.app',
  appName: 'my-react-capacitor-app',
  webDir: 'build'
};

export default config;
